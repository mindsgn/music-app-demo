import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    position: 'absolute',
    width: '100%',
    height: 100,
    top: 0,
    zIndex: 1,
  },
  blurContainer: {
    position: 'relative',
    width: '100%',
    height: '100%',
    padding: 10,
  },
});

export default styles;
