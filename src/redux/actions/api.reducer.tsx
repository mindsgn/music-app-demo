import {ARTIST, ALBUM, PLAYLIST} from '../../constants';

const APIAction = (props: any) => {
  const {dispatch} = props;

  const getArtist = () => {
    try {
    } catch (error) {}
  };

  const getPlaylist = () => {
    try {
    } catch (error) {}
  };

  const getAlbum = () => {
    try {
    } catch (error) {}
  };

  const getRandom = () => {
    try {
    } catch (error) {}
  };

  return {getArtist, getPlaylist, getAlbum, getRandom};
};

export default APIAction;
